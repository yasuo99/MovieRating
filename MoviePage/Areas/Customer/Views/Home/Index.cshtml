@{
    ViewData["Title"] = "Home Page";
}
@model MoviePage.Areas.Customer.Dtos.HomeDTO
<div class="container">
    <div class="movie-items">
        @foreach(var movie in Model.Movies)
            {

            }
    </div>
</div>
